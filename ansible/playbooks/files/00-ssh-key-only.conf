# Disable password SSH auth. Must be read before 50-cloud-init.conf (sshd uses first value).
# Deploy as 00-ssh-key-only.conf so it wins over cloud-init's PasswordAuthentication yes.
PasswordAuthentication no
KbdInteractiveAuthentication no
