# x260 home server inventory
# Usage: ansible -i inventory/hosts.yml x260 -m ping

all:
  children:
    x260_servers:
      hosts:
        x260:
          ansible_host: 192.168.0.118
          ansible_user: gleb
          # ansible_ssh_private_key_file: ~/.ssh/id_ed25519  # uncomment if needed
